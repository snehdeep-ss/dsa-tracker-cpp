#pragma once
// Sheet — Domain Model
// Responsibility: Plain data struct. No logic, no I/O.

#ifndef SHEET_H
#define SHEET_H

#include <string>
#include <vector>
#include <ctime>

struct Sheet {
    // TODO: add fields
};

#endif // SHEET_H
