#pragma once
// Tag — Domain Model
// Responsibility: Plain data struct. No logic, no I/O.

#ifndef TAG_H
#define TAG_H

#include <string>
#include <vector>
#include <ctime>

struct Tag {
    // TODO: add fields
};

#endif // TAG_H
