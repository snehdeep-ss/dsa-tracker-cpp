#pragma once
// Submission — Domain Model
// Responsibility: Plain data struct. No logic, no I/O.

#ifndef SUBMISSION_H
#define SUBMISSION_H

#include <string>
#include <vector>
#include <ctime>

struct Submission {
    // TODO: add fields
};

#endif // SUBMISSION_H
