#pragma once
// UserIntegration — Domain Model
// Responsibility: Plain data struct. No logic, no I/O.

#ifndef USER_INTEGRATION_H
#define USER_INTEGRATION_H

#include <string>
#include <vector>
#include <ctime>

struct UserIntegration {
    // TODO: add fields
};

#endif // USER_INTEGRATION_H
