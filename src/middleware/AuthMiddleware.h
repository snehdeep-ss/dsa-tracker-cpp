#pragma once
// AuthMiddleware
// Responsibility: TODO

#ifndef AUTH_MIDDLEWARE_H
#define AUTH_MIDDLEWARE_H

class AuthMiddleware {
public:
    AuthMiddleware();
    ~AuthMiddleware();
};

#endif // AUTH_MIDDLEWARE_H
